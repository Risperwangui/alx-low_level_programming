#include "main.h"

/**
 * _strcpy - copies the string pointed to src, including
 * @src: pointer
 * @dest: pointer
 * Return: the pointer to dest
 */

char *_strcpy(char *dest, char *src)

{

	int i, index;



	for (index = 0; src[index] != '\0'; index++)

	{

	}

	for (i = 0; i <= index; i++)

	{

		dest[i] = src[i];

	}

	return (dest);

}
